{
  "name": "jacksonmooring.com",
  "scripts": {
    "setup": "bin/setup",
    "dev": "sudo jekyll serve --drafts -d ./docs",
    "build": "sudo jekyll build -d ./docs"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "npm": "^6.0.1",
    "retinajs": "^2.1.1",
    "svgxuse": "^1.2.4",
    "webfontloader": "^1.6.28",
    "zooming": "^2.0.0"
  }
}
